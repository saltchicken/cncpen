globals:
  outline_simplify: 0.05
  no_outline: false

fills:
  # Base structural rings
  - operation: concentric
    spacing: 1.5
    ring_simplify: 0.1
  
  # Dense internal complexity
  - operation: voronoi
    density: 50
    seed: 42
    
  # Minimize travel time between the scattered Voronoi cells
  - operation: optimize
